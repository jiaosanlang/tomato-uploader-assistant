const $=id=>document.getElementById(id);const state={step:'source',files:[],chapters:[],schedule:[],page:null};const steps=['source','schedule','page'];const chapterPattern=/^第[一二三四五六七八九十百千万零〇两0-9０-９]+[章节回]\s*(.*)$/;
function setStep(step){state.step=step;steps.forEach((name,index)=>{ $(`${name}View`).classList.toggle('active',name===step);const marker=document.querySelector(`[data-step="${name}"]`);marker.classList.toggle('active',name===step);marker.classList.toggle('done',index<steps.indexOf(step))});$('prevButton').disabled=steps.indexOf(step)===0;$('nextButton').disabled=steps.indexOf(step)===steps.length-1}function setFooter(message){$('footerStatus').textContent=message}function escapeHtml(value){return String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]))}
function parseChapterNumber(title){const match=title.match(/^第\s*([0-9０-９一二三四五六七八九十百千万零〇两]+)\s*[章节回]/);if(!match)return 0;const digits=match[1].replace(/[０-９]/g,char=>String.fromCharCode(char.charCodeAt(0)-0xfee0));if(/^\d+$/.test(digits))return Number(digits);const map={零:0,〇:0,一:1,二:2,两:2,三:3,四:4,五:5,六:6,七:7,八:8,九:9};let total=0,section=0;for(const char of digits){if('十百千万'.includes(char)){const unit={十:10,百:100,千:1000,万:10000}[char];section=(section||1)*unit;if(unit===10000){total+=section;section=0}}else section+=map[char]??0}return total+section}
async function readFile(file){return{name:file.webkitRelativePath||file.name,text:await file.text()}}function extractChapters(files){const chapters=[];for(const file of files){const lines=file.text.replace(/\r\n?/g,'\n').split('\n');let current=null;const push=()=>{if(!current)return;current.content=current.content.join('\n').trim();current.chars=(current.content.match(/[一-鿿]/g)||[]).length;if(current.content)chapters.push(current);current=null};for(const raw of lines){const line=raw.trim();if(chapterPattern.test(line)){push();current={title:line,number:parseChapterNumber(line),content:[],source:file.name}}else if(current)current.content.push(raw)}push();if(!chapters.some(ch=>ch.source===file.name)&&file.text.trim()){const title=file.name.replace(/\.txt$/i,'').trim();chapters.push({title,number:parseChapterNumber(title),content:file.text.trim(),chars:(file.text.match(/[一-鿿]/g)||[]).length,source:file.name})}}return chapters.sort((a,b)=>(a.number||Number.MAX_SAFE_INTEGER)-(b.number||Number.MAX_SAFE_INTEGER)||a.source.localeCompare(b.source,'zh-CN',{numeric:true}))}
function addDays(date,amount){const next=new Date(date);next.setDate(next.getDate()+amount);return next}function fmtDate(date){const p=n=>String(n).padStart(2,'0');return`${date.getFullYear()}-${p(date.getMonth()+1)}-${p(date.getDate())}`}function fmtTime(date){return`${String(date.getHours()).padStart(2,'0')}:${String(date.getMinutes()).padStart(2,'0')}`}function computeSchedule(chapters){const dailyLimit=Math.max(1,Number($('dailyWordLimit').value)||10000);const chapterLimit=Math.max(1,Number($('dailyChapterLimit').value)||3);const start=$('startDate').value?new Date(`${$('startDate').value}T00:00:00`):new Date();const[hours,minutes]=($('dailyPublishTime').value||'07:00').split(':').map(Number);let day=new Date(start),words=0,count=0;return chapters.map(chapter=>{if(count>=chapterLimit||(words+chapter.chars>dailyLimit&&words>0)){day=addDays(day,1);words=0;count=0}const date=new Date(day);date.setHours(hours,minutes,0,0);words+=chapter.chars;count++;return{...chapter,date:fmtDate(date),time:fmtTime(date)}})}
function renderChapters(){$('chapterCount').textContent=`${state.chapters.length} 章`;$('chapterPreview').innerHTML=state.chapters.slice(0,30).map((chapter,index)=>`<div class="preview-item"><span class="index">${String(index+1).padStart(2,'0')}</span><span class="title">${escapeHtml(chapter.title)}</span><small>${chapter.chars} 字</small></div>`).join('')}function renderSchedule(){const days=new Set(state.schedule.map(chapter=>chapter.date));$('dayCount').textContent=`${days.size} 天`;const totalChars=state.schedule.reduce((sum,chapter)=>sum+chapter.chars,0);$('scheduleSummary').className='summary-card';$('scheduleSummary').innerHTML=`<strong>${state.schedule.length} 章</strong><span>${totalChars} 字 · ${days.size} 天完成 · 每天最多 ${$('dailyChapterLimit').value||3} 章</span>`;$('scheduleList').innerHTML=state.schedule.slice(0,60).map(chapter=>`<div class="schedule-item"><span class="date">${chapter.date}</span><span class="title">${escapeHtml(chapter.title)}</span><small>${chapter.time}</small></div>`).join('')}
async function sendToPage(message){const[tab]=await chrome.tabs.query({active:true,currentWindow:true});if(!tab?.id||!/^https:\/\/(fanqienovel\.com|writer\.fanqienovel\.com)\//.test(tab.url||''))throw new Error('当前标签页不是番茄作者后台页面。');return chrome.tabs.sendMessage(tab.id,message)}
async function selectFiles(event){state.files=await Promise.all([...event.target.files].filter(file=>/\.txt$/i.test(file.name)).map(readFile));$('sourceStatus').textContent=`已选择 ${state.files.length} 个 TXT 文件，点击“解析章节”。`;setFooter('已选择正文')}$('novelFiles').addEventListener('change',selectFiles);$('novelDirectory').addEventListener('change',selectFiles);$('parseButton').addEventListener('click',()=>{if(!state.files.length){$('sourceStatus').textContent='请先选择 TXT 文件或正文目录。';return}state.chapters=extractChapters(state.files);state.schedule=computeSchedule(state.chapters);renderChapters();renderSchedule();$('sourceStatus').textContent=`已识别 ${state.chapters.length} 章。`;setFooter('章节已解析')});$('scheduleButton').addEventListener('click',()=>{state.schedule=computeSchedule(state.chapters);renderSchedule();setFooter('排期已更新')});
$('inspectButton').addEventListener('click',async()=>{try{state.page=await sendToPage({type:'inspect-page'});$('connectionDot').classList.add('connected');$('pageBadge').textContent='已连接';$('pageInfo').className='page-card';$('pageInfo').innerHTML=`<strong>${escapeHtml(state.page.title||'未命名页面')}</strong><code>${escapeHtml(state.page.url)}</code><p>${state.page.inputs.length} 个输入项 · ${state.page.buttons.length} 个按钮</p>`;$('debugOutput').textContent=JSON.stringify(state.page,null,2);$('fillButton').disabled=!state.schedule.length;setFooter('页面已识别')}catch(error){$('pageInfo').textContent=error.message;setFooter('连接失败')}});$('fillButton').addEventListener('click',async()=>{if(!state.schedule.length)return;try{const result=await sendToPage({type:'fill-chapter',chapter:state.schedule[0]});if(!result.ok)throw new Error(result.error||'填写失败');$('pageInfo').innerHTML=`<strong>${escapeHtml(result.message)}</strong><p>标题：${escapeHtml(result.title||'')}</p><p>正文：${result.contentLength||0} 字</p>`;setFooter('已填写，请检查页面')}catch(error){setFooter(error.message)}});$('prevButton').addEventListener('click',()=>setStep(steps[Math.max(0,steps.indexOf(state.step)-1)]));$('nextButton').addEventListener('click',()=>setStep(steps[Math.min(steps.length-1,steps.indexOf(state.step)+1)]));
(async()=>{const fields=['dailyWordLimit','dailyChapterLimit','startDate','dailyPublishTime'];const saved=await chrome.storage.local.get(fields);for(const key of fields){if(saved[key]!=null)$(key).value=saved[key];$(key).addEventListener('change',()=>chrome.storage.local.set({[key]:$(key).value}))}setStep('source')})();

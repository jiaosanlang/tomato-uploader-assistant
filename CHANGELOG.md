# 更新记录

本项目遵循语义化版本的基本约定。

## 0.4.0

1. 撤销会导致部分 Chrome 环境崩溃的侧边栏目录直接读取
2. 正文目录改由本地扫描助手选择、校验和解析
3. 按“从第几章开始”过滤后再向扩展返回章节，降低浏览器内存压力
4. 增加 2000 个文件、单文件 10 MB、总计 30 MB 的强制安全限制
5. 增加项目级崩溃预防规则与 `check:extension-safety` 自动检查

使用目录功能前需要先双击 `start-extension-helper.bat`。单独选择少量 TXT 仍可直接在扩展中完成。

## 0.3.2

1. 修复 Chrome 侧边栏选择目录后没有返回结果的问题
2. 改用原生目录文件输入，选择后自动扫描目录及子目录中的 TXT
3. 修复相对目录路径导致章节号无法识别的问题
4. 支持识别带 Markdown `#` 的章节标题，并压缩正文多余空行
5. 修复中文章节数字在“从第几章开始”筛选时的计算错误

## 0.3.1

1. 移除 Chrome 扩展对本地 Node 助手的依赖
2. 扩展不再访问 `127.0.0.1`，也不要求启动 `start.bat`
3. 恢复纯 Chrome 目录选择和逐个 TXT 读取

本版本不要求启动 `start.bat`。项目中的本地客户端仍可单独使用，但与 Chrome 扩展互不依赖。

## 0.3.0

1. 目录扫描迁移到本地 Node.js 助手
2. Chrome 不再直接读取整个正文目录
3. 扩展通过本地接口调用 Windows 文件夹选择器
4. 保留章节清理、“从第几章开始”和浏览器页面填写功能

## 0.2.3

1. 统一使用可见的目录文件选择入口，兼容 Chrome 侧边栏目录选择
2. 去掉目录选择器的 TXT 类型过滤，避免 Windows 目录窗口误导
3. 目录没有返回 TXT 时显示明确提示
4. 没有识别章节时禁用“下一步”

## 0.2.2

1. 修复目录导入一次性读取全部文件可能导致浏览器崩溃的问题
2. 改用目录句柄逐个扫描和读取 TXT
3. 增加目录导入进度、取消操作和大小/数量限制
4. 增加“从第几章开始”筛选
5. 清理正文中的重复标题、Markdown 标记和异常空格

## 0.2.0

1. 新增 Chrome Manifest V3 侧边栏扩展骨架
2. 支持浏览器内选择 TXT、解析章节和生成排期
3. 支持识别当前番茄作者后台页面
4. 支持向当前章节编辑页填写标题与正文
5. 发布确认暂时保留人工操作

## 0.1.0

初始开源版本。

1. 支持整本 TXT 和分章目录解析
2. 支持章节字数统计与发布排期
3. 提供本地可视化操作页面
4. 支持 Playwright 登录状态保存
5. 支持章节提交校验和断点记录
6. 默认采用单章安全上传模式
